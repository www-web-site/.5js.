const askName = prompt('Ismingiz:');
const askId = prompt('Space ID:');
const askPass = prompt('Space Parol:');
const info = {
    name: askName,
    id: askId,
    password: askPass,
}

console.log(info);